{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    {
      "src": "/control-gastos/(.*)",
      "dest": "/control-gastos/index.html"
    }
  ]
}
